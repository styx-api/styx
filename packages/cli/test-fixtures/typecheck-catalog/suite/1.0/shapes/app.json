{
  "name": "shapes",
  "source": {
    "type": "boutiques",
    "path": "boutiques.json"
  },
  "docs": {
    "description": "Hand-written tool covering the common scalar/flag/list/optional shapes - notably optional-without-default fields, which the params factory omits and which must be typed typing.NotRequired so cargs/outputs reads stay KeyError-safe and mypy-clean."
  }
}
